# Media and derived artefacts
# Per-clip detection/tracking intermediates from tools/detect_players_data2.py.
# Expensive to regenerate (~11 min a clip on CPU) but strictly derived — the
# committed deliverable is outputs/player_counts_data_2.csv plus its provenance.
outputs/data_2_detect/
*.parquet
data/
*.mp4
*.mkv
*.mov
*.jpg
*.jpeg
*.png
!outputs/figures/*.png

# Raw participant data — never commit this
pilot/sessions/*
!pilot/sessions/.gitkeep
pilot/*_signed*
pilot/recordings/

# Filled-in judgements are kept; the working copy is not
eval/queries.yaml

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.env
.pytest_cache/
.ipynb_checkpoints/

# OS / editor
.DS_Store
.idea/
.vscode/
